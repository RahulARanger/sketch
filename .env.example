# Google OAuth client ID for the desktop app's Google Identity Services flow.
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# Required when Google requires it during token exchange. Not a protected secret in a desktop app.
VITE_GOOGLE_CLIENT_SECRET=your-client-secret
